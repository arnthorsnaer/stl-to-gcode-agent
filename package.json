{
  "name": "stl-to-gcode-agent",
  "version": "0.2.0",
  "type": "module",
  "private": true,
  "description": "Small Bun scripts for turning local STL/ZIP or direct URLs into Ender-3 V2 Neo G-code with PrusaSlicer.",
  "scripts": {
    "import": "bun app/scripts/import.ts",
    "slice": "bun app/scripts/slice.ts",
    "convert": "bun app/scripts/convert.ts",
    "preview": "bun app/scripts/preview.ts",
    "list": "bun app/scripts/list.ts"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
